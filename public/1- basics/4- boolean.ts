/*
 * boolean türü yalnızca true veya false değerlerini alabilen
 * basit bir js veri türüdür.
 * not : karşılatırmalar true veya false dödürür
 */

let age: number = 20;

let ehilyetAlabilir: boolean = false;

console.log(ehilyetAlabilir);

// karşılaştırma örneği
let karsilastirma: boolean = age > 18;

console.log(karsilastirma);
